/** Authoritative work factor for every password hash created by the API. */
export const BCRYPT_ROUNDS = 12;

/** Entropy used for the opaque password required by the current User schema. */
export const OAUTH_BOOTSTRAP_SECRET_BYTES = 32;
