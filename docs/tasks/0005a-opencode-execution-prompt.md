# OpenCode Task Prompt: Complete Inbox Acceptance

Implement `docs/tasks/0005a-fix-inbox-acceptance.md` in the current repository.

You are authorized to inspect the codebase and autonomously create or modify any project files
needed to complete this correction. Do not ask the user for confirmation for in-repository
changes. Preserve unrelated work, do not change Product Bible policy, and do not modify external
systems.

Use your normal engineering workflow. Understand the existing React Query and NestJS testing
patterns, choose a focused implementation, make the changes, and verify the real behavior. The
task is complete only when the Inbox cache mutation is correct, mobile TypeScript passes, and the
new tests exercise production HTTP/hook/component code rather than copied simulations.

Run the verification commands from the task, fix in-scope failures, and provide a concise final
report with changed files, test counts, command results, and residual risks. Stop after Task
0005A; Task 0006 is a separate follow-up.
