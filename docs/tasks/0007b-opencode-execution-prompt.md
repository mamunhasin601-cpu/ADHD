# OpenCode Execution Prompt: Task 0007B

Execute `docs/tasks/0007b-auth-evidence-and-documentation.md` now.

This repository work is pre-approved. You are authorized to create or modify any files inside
the repository required by the task without asking the user for permission or confirmation.
Preserve unrelated changes. Do not publish, deploy, push, modify external systems, or change
Product Bible policy.

Work autonomously. Implement the task completely; do not stop after analysis or a plan. Fix the
auth integration test evidence and synchronize the specified engineering documents. Do not add
new product features and do not rewrite unrelated source code.

Use the exact verification commands in the task. Do not use `--forceExit`, skipped tests,
artificial timeouts, or weakened assertions. If PostgreSQL, Redis, Docker, or a device is
unavailable, report it as **not verified** rather than claiming success.

Before finishing, inspect the diff for stale `132/156` current-result claims and for Product Bible
changes. Product Bible policy files must remain unchanged by this task.

Report:

- files changed;
- exact command results and test counts;
- confirmation that the two-user JWT identity path is proven;
- documentation sections updated;
- unavailable checks and residual risks.

Stop after the implementation and verification report.
