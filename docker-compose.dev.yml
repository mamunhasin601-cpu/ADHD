# Оверрайд для разработки: подключает volumes для hot-reload
# Запускать через: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
services:
  postgres:
    environment:
      POSTGRES_LOG_STATEMENT: "all"

  redis:
    command: redis-server --loglevel verbose
